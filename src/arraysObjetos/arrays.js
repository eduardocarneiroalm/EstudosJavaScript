// arrays

const numbers = [3, 2, 4, 30, 2, 1, 8];

// adicionar um valor para dentro da variável .push()

numbers.push(9)
 console.log(numbers)

 // removendo item dentro do array

 numbers.pop(); // posso colocar ele dentro de uma variável, que ai vai mostrar qual ele tirou
 console.log(numbers)

// remover o primeiro da lista .shift

const firstName = numbers.shift();
console.log(numbers)
console.log(firstName)

// adiciona no começo

numbers.unshift(50)
console.log(numbers)
